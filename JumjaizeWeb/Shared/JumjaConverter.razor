<h3>Jumjaize - Korean Braille Converter</h3>

<p>단순 변환: 한글 점자의 규칙을 모두 무시하고, 단순히 한글과 점자를 1:1로 변환</p>
<p>점자 변환: 한글 점자의 규칙을 따르면서 한글을 점자로 변환 (규칙 미완성)</p>

<div class="container">
    <div class="row">
        <div class="col-sm">
            <textarea class="form-control" rows="10" @bind="inputText"></textarea>
        </div>
        <div class="col-">
        </div>
        <div class="col-sm">
            <textarea class="form-control" rows="10" @bind="jumjaResult"></textarea>
        </div>
    </div>
    <div class="row">
        <div class="col-2">
            <button class="btn btn-primary" @onclick="ConvertJumjaWithoutRules">단순 변환</button>
        </div>
        <div class="col-2">
            <button class="btn btn-primary" @onclick="ConvertJumja">점자 변환</button>
        </div>
    </div>
</div>

@code {
    private string inputText = "한글\n아이\n우유\n어머니\n아버지";
    private string jumjaResult;

    private async Task ConvertJumjaWithoutRules(MouseEventArgs e)
    {
        await Task.Run(() =>
        {
            var jumjaize = new Jumjaize.Jumjaize();
            jumjaResult = jumjaize.ToJumjaWithoutRules(inputText);
        });
    }

    private async Task ConvertJumja(MouseEventArgs e)
    {
        await Task.Run(() =>
        {
            var jumjaize = new Jumjaize.Jumjaize();
            jumjaResult = jumjaize.ToJumja(inputText);
        });
    }
}
